<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.webprj.project_green.dao.BoardDao" >

	<insert id="join" parameterType="com.webprj.project_green.dto.CustomDto" >
    	INSERT INTO CUSTOMER (ID, PASSWORD,NAME, EMAIL, PHONE)
		VALUES(#{id}, #{password},#{name}, #{email}, #{phone})
    </insert>
    
    <select parameterType="String" id="logins" resultType="String">
    SELECT PASSWORD FROM CUSTOMER WHERE ID=#{id} 
    </select>
    
    <select id="idcheck" parameterType="String" resultType="String">
      SELECT COUNT(*) FROM tbl_member
      WHERE
          id = #{id}
</select>
    
    
</mapper>